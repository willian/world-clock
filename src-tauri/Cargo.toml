[package]
name = "world-clock"
version = "0.0.0"
description = "A menu bar World Clock for macOS"
edition = "2024"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
core-graphics = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-single-instance = "2"
